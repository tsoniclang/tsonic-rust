[package]
name = "acme-widget"
version = "1.2.3"
edition = "2021"

[lib]
name = "acme_widget"
path = "src/lib.rs"

[features]
default = []
extras = []

[dependencies]
tsonic_rust_runtime = { path = "../../../../../rust-runtime/crates/tsonic_rust_runtime" }
